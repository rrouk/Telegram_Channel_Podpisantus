# ✍️ Telegram Channel Signature Bot 

**Telegram Channel Signature Bot** — это асинхронный бот, который автоматически добавляет и управляет настраиваемыми подписями (футерами) к новым постам в ваших Telegram-каналах. Бот немедленно редактирует опубликованное сообщение, объединяя оригинальный текст с сохраненной подписью.

### Рабочая версия бота находится тут: [@Podpisantus_bot](https://t.me/@Podpisantus_bot)

## ✨ Основные возможности

* **Автоматическое редактирование постов**: Бот мгновенно добавляет подпись к новым текстовым постам, а также к подписям медиафайлов (фото, видео, документы, аудио).
* **Сохранение форматирования**: Поддерживается сохранение **текста** и **всего форматирования** подписи (жирный, курсив, ссылки и т.д.) благодаря сериализации сущностей (`MessageEntity`).
* **Учет лимитов Telegram**: Бот напоминает пользователю об ограничениях по длине постов (4096 символов для текста, 1024 для медиа-подписей).
* **Раздельное управление**: Пользователи могут управлять только теми каналами, которые они добавили в базу данных, обеспечивая изоляцию данных.

### 🤖 Команды администратора

| Команда | Описание |
| :--- | :--- |
| `/add` | Добавить новый канал в базу бота и установить для него подпись. |
| `/edit` | Изменить существующую подпись для канала (с предпросмотром текущей). |
| `/list` | Показать список всех каналов, которыми управляет текущий пользователь. |
| `/delete` | Удалить канал и его подпись из базы данных бота. |
| `/info` | Показать информацию об ограничениях по длине постов. |

## ⚙️ Технологии

| Компонент | Технология | Описание |
| :--- | :--- | :--- |
| **Фреймворк** | `pyTelegramBotAPI` (`telebot`) | Основной интерфейс для работы с Telegram API. |
| **База данных** | `sqlite3` | Хранение ID каналов, ID администраторов, текста подписи и форматирующих сущностей. |
| **Сериализация** | `json` | Использование JSON для сохранения и восстановления сложного объекта `MessageEntity`.
| **Текст** | UTF-16 Code Units | Точное вычисление смещений (`offset`) для корректного добавления форматирования в измененный пост. |

## 🚀 Установка и Запуск

1.  **Токен**: Установите токен вашего бота как переменную окружения `TG_BOT_TOKEN` (например, в файле `.env`).
2.  **Инициализация DB**: При запуске автоматически создается файл базы данных `/app/channel_signatures.db` и таблица `channels`.
3.  **Запуск**:
    ```bash
    # Убедитесь, что все зависимости установлены
    python bot.py
    ```
4.  **Права доступа**: Добавьте бот в целевой канал и **обязательно** выдайте ему права администратора на **Редактирование сообщений**.
5.  **Начало работы**: Отправьте боту команду `/start`, а затем `/add`, чтобы начать управление каналами.
